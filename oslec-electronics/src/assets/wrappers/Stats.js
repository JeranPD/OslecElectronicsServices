import styled from "styled-components";

const Wrapper = styled.section`
  .stats-section {
    height: 100vh;
  }

  h4 {
    text-align: center;

    margin-top: 5em;
  }
`;

export default Wrapper;
